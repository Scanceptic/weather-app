/*
    Synchronous
    Takes in weather JSON object
    Returns new object with only the required data
*/

export function processWeatherJSON(weatherJSON) {
	// process JSON
	const processedJSON = weatherJSON;
	// return processed data
	return processedJSON;
}
